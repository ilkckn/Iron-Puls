import "./Rebuilt.css"

const Rebuilt = () => {
  return (
    <section className="rebuilt-container">
      <h1>ready to be rebuilt?</h1>
      <p>
        Our evaluation process is rigorous. We only take on athletes who are
        committed to the process. Excellence is not optional.
      </p>
      <button>apply for coaching</button>
    </section>
  );
};

export default Rebuilt;
